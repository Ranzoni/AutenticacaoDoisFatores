# This workflow will build a .NET project
# For more information see: https://docs.github.com/en/actions/automating-builds-and-tests/building-and-testing-net

name: .NET

on:
  push:
    branches: [ "main", "develop" ]
  pull_request:
    branches: [ "main", "develop" ]

jobs:
  build:

    runs-on: ubuntu-latest
    env:
      ADF_CHAVE_AUTENTICACAO: "@TESTE_DE_CH@V&_DE_@UTHENTICAO.____(((FS78N3R20F239J88U@@@111...DSFI10HJ80UTESTE_DE_CH@V&_DE_@UTHENTICAO.____(((FS78N3R20F239J88"

    steps:
    - uses: actions/checkout@v4
    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 9.0.x
    - name: Restore dependencies
      run: dotnet restore AutenticacaoDoisFatores
    - name: Build
      run: dotnet build AutenticacaoDoisFatores --no-restore
    - name: Test
      run: dotnet test AutenticacaoDoisFatores --no-build --verbosity normal
